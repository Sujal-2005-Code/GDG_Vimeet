# Backend API base URL (no trailing slash). Set this in Vercel's project
# environment variables to your Render backend's URL, e.g.:
# VITE_API_BASE_URL=https://gdg-vimeet.onrender.com
VITE_API_BASE_URL=
